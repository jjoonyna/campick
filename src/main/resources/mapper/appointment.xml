<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.choongang.campick.mapper.AppointmentDAO">


	
	<!-- 회원 예약 조회 -->
	<insert id="insertuser2" parameterType="cmp_user2">
		INSERT INTO cmp_user(
			apt_no,
			cmp_no,
			user_id,
			user_nm,
			apt_date,
			apt_pp,
			apt_req,
			apt_staydate,
			apt_price,
			apt_at
		)VALUES(
			#{apt_no},
			#{cmp_no},
			#{user_id},
			#{user_nm},
			#{apt_date},
			#{apt_pp},
			#{apt_req},
			#{apt_staydate},
			#{apt_staydate},
			#{apt_at},
			'u2')
		</insert>
	
	<!-- 회원 예약 삭제 -->
	<update id="deleteUser2" parameterType="String">
		UPDATE cmp_user
		SET
		user_kind = 'du2'
		WHERE user_id=#{user_id}
	</update>
	
	<!-- 사업자 예약 조회 -->
<insert id="insertBiz2" parameterType="cmp_user2">
		INSERT INTO cmp_user(
			apt_no,
			cmp_no,
			user_id,
			user_nm,
			apt_date,
			apt_pp,
			apt_req,
			apt_staydate,
			apt_price,
			apt_at
		)VALUES(
			#{apt_no},
			#{cmp_no},
			#{user_id},
			#{user_nm},
			#{apt_date},
			#{apt_pp},
			#{apt_req},
			#{apt_staydate},
			#{apt_staydate},
			#{apt_at},
			'b2')
		</insert>

	<!-- 사업자 예약 삭제 -->
	<update id="deleteBiz2" parameterType="String">
		UPDATE cmp_user
		SET
		user_kind = 'db2'
		WHERE user_id=#{user_id}
	</update>











